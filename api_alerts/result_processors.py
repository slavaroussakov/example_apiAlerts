"""Houses classes responsible for using the data generated by checks
Currently there is one processor that outputs to stdout. Others may
be added that send data to other services like Splunk or PagerDuty."""
import json
from abc import abstractmethod


class ResultProcessor():
    """This is meant to function as an interface for checks to use.
    Checks shouldn't be aware of individual result processor
    implementation"""

    @abstractmethod
    def process(self):
        """Logic for reporting data should be filled in by each separate
        result processor implementation"""


class StdoutProcessor(ResultProcessor):
    """Simple result processor to print output to stdout"""

    def process(self, check_data):
        """Serialize data and print to stdout"""
        print(json.dumps(check_data))
